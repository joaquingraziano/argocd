apiVersion: argoproj.io/v1alpha1
kind: Deployment
metadata:
  name: webdemo-deployment
spec:
  selector:
    matchLabels:
      app: webdemo-dev
  replicas: 2 # Puedes cambiar esto según tus necesidades
  template:
    metadata:
      labels:
        app: webdemo-dev
    spec:
      containers:
        - name: webdemo-dev
          image: jgraziano/webdemo-dev:v1.10
          ports:
            - containerPort: 80 # Puedes cambiar esto según las necesidades de tu aplicación


## minikube dashboard
## kubectl delete ns dev
## kubectl get ns
##  kubectl create namespace dev
## kubectl get svc -n dev
## minikube ip
## http://192.168.49.2:32683/